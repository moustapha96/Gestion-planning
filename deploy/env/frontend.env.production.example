# Frontend — build production (tech-xuma.com)
# Copier vers frontend/.env.production avant : npm run build
#
# L'API est sur un sous-domaine distinct du frontend.

VITE_API_URL=https://back-gp.tech-xuma.com
VITE_APP_TIMEZONE=UTC
